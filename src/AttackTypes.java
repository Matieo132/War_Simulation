/**
 * Enum {@code AttackTypes} reprezentuje różne typy ataku dostępne w symulacji bitwy.
 * Dostępne typy to: NORMAL (zwykły atak), EXPLOSION (wybuchowy atak), HEAL (leczenie).
 */
public enum AttackTypes {
    NORMAL, EXPLOSION, HEAL;
}
