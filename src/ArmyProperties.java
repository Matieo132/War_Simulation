/**
 * Enum {@code ArmyProperties} reprezentuje właściwości jednostki w symulacji bitwy.
 * Dostępne właściwości to: HP (punkty życia), RANGE (zasięg ataku).
 */
public enum ArmyProperties {
    HP, RANGE
}
